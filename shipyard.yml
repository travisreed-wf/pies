---
base: 'ubuntu/trusty'

apt:
  packages:
    - gcc

add:
  load.sh: /usr/local/bin/load.sh
  load.c: /load/load.c

after_install:
  - gcc -o /usr/local/bin/load /load/load.c

cmd: [ '/usr/local/bin/load.sh' ]
