project: pub
language: pub

script:
  - echo 'pies'
  - cd pies2 && git ls-files --cached --others --exclude-standard . | tar -zcvf pies2.tar.gz --files-from -
  - cd pies3 && git ls-files --cached --others --exclude-standard . | tar -zcvf pies3.tar.gz --files-from -

artifacts:
  pub:
    - pies*.tar.gz
